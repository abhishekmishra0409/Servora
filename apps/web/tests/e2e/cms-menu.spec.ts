import { test } from '@playwright/test';

test.skip(true, 'Requires seeded auth and local web services.');

test('cms user can create a menu item', async () => {
  // Intentionally skipped until local services are wired in CI.
});

