import { test } from '@playwright/test';

test.skip(true, 'Requires running local web and API services.');

test('customer can add a bucket item', async () => {
  // Intentionally skipped until local services are wired in CI.
});

