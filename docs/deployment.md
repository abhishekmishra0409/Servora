# Deployment

- Deploy `apps/api`, `apps/realtime`, and `apps/worker` as separate Node services.
- Deploy `apps/web` on a Next.js-capable platform.
- Deploy `apps/waiter` and `apps/kitchen` as static assets.
- Use managed MongoDB and Redis in production.
- Keep webhook endpoints publicly reachable and protected by signature verification.

